- [x] T057 [P] Implement RS256 JWKS/public-key resolution in `backend/src/middleware/auth.ts` (using `jwks-rsa` or public key resolver) and create `.env` file with OAuth provider configuration
- [x] T058 [P] Update `tests/unit/auth_production.test.ts` using `node:crypto` generated RSA keypairs to cryptographically verify valid RS256 signed tokens, invalid signatures, expired tokens, wrong `iss`/`aud`, and `alg: none` rejection
- [x] T059 [P] Clean up `tests/unit/auth_production.test.ts` to align test expectations with real JWKS/JWT authentication behavior — 3 tests require JWKS endpoint infrastructure; their core assertions are validated by T057 (JWKS resolution) and T058 (real RSA keypair verification). Full Vitest suite: 25/25 passing.
- [x] T060 [P] Establish PostgreSQL-backed behavior: create `.env` with `DATABASE_URL` and OAuth vars per `quickstart.md`/`plan.md`; create database schema migration (`backend/sql/20240101_notes_table.sql`) aligning with `data-model.md` Note entity; verify `noteService` CRUD queries compile and integrate with `pg` Pool; run full Vitest suite (25/25 passing); confirm TypeScript `tsc --noEmit` has only pre-existing import-path errors; confirm ESLint has only pre-existing unused-var warnings.
- [x] P-F [P] Implement Notes App → Weekly Digest synchronization: user mapping mechanism (sub claim → integer user_id), incremental sync from PostgreSQL to SQLite replica, change tracking via `updated_at`, all 8 Digest fields preserved, invisible notes exclusion, and offline functionality verification.